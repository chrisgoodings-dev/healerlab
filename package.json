{
  "name": "healerlab",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A World of Warcraft healer progression planner built with HTML, CSS and JavaScript.",
  "scripts": {
    "dev": "node dev-server.mjs",
    "build": "node scripts/build.mjs",
    "preview": "SERVE_DIR=dist node dev-server.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
